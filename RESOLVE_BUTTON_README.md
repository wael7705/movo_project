# زر بسط - حل مشاكل الطلبات

## الميزة الجديدة

تم إضافة زر "بسط" في تبويب المشكلة لتمكين المستخدم من اختيار حالة الطلب الجديدة ونقله للتبويب المطلوب.

## الوظائف

### 1. زر بسط في تبويب المشكلة
- **الموقع**: يظهر فقط في تبويب "مشكلة"
- **اللون**: أخضر مع أيقونة 🔧
- **الوظيفة**: فتح مودال لاختيار الحالة الجديدة

### 2. مودال اختيار الحالة
- **العنوان**: "اختر حالة الطلب الجديدة"
- **المحتوى**: قائمة بجميع الحالات المتاحة
- **الحالات المتاحة**:
  - قيد الانتظار (pending)
  - تعيين كابتن (choose_captain)
  - معالجة (processing)
  - خارج للتوصيل (out_for_delivery)
  - تم التوصيل (delivered)
  - ملغي (cancelled)
  - مؤجل (deferred)

### 3. API الجديد
- **Endpoint**: `PATCH /api/v1/orders/{order_id}/resolve`
- **المعاملات**: `{"status": "new_status"}`
- **التحقق**: يتأكد من أن الطلب في حالة مشكلة
- **النتيجة**: نقل الطلب للحالة المطلوبة

## الملفات المحدثة

### Frontend
1. **`movo-ts/src/components/StatusSelectionModal.tsx`** - مودال اختيار الحالة
2. **`movo-ts/src/components/OrderCard.tsx`** - إضافة زر بسط
3. **`movo-ts/src/pages/Dashboard.tsx`** - إضافة handlers للمودال
4. **`movo-ts/src/lib/api.ts`** - إضافة API function

### Backend
1. **`backend/api/routes/orders.py`** - إضافة endpoint `/resolve`

## كيفية الاستخدام

### للمستخدم
1. انتقل إلى تبويب "مشكلة"
2. ستجد زر "بسط" 🔧 في كل بطاقة طلب
3. اضغط على الزر لفتح مودال اختيار الحالة
4. اختر الحالة الجديدة من القائمة
5. اضغط "تأكيد" لنقل الطلب

### للمطور
```typescript
// استدعاء API
await api.orders.resolve(orderId, newStatus);

// فتح المودال
setStatusSelectionModal({
  isOpen: true,
  orderId: orderId,
  currentStatus: currentStatus
});
```

## المزايا

1. **سهولة الاستخدام**: واجهة بسيطة وواضحة
2. **المرونة**: اختيار أي حالة من الحالات المتاحة
3. **الأمان**: التحقق من صحة الحالة قبل التطبيق
4. **التكامل**: يعمل مع النظام الحالي بدون كسر
5. **التعريب**: دعم كامل للغة العربية

## الاختبار

### API Test
```bash
# وضع طلب في حالة مشكلة
curl -X PATCH http://localhost:8000/api/v1/orders/1/problem

# حل المشكلة ونقل الطلب لـ pending
curl -X PATCH http://localhost:8000/api/v1/orders/1/resolve \
  -H "Content-Type: application/json" \
  -d '{"status": "pending"}'
```

### Frontend Test
1. انتقل إلى `http://localhost:3000`
2. اذهب لتبويب "مشكلة"
3. اضغط على زر "بسط" في أي طلب
4. اختر حالة جديدة
5. تأكد من نقل الطلب للتبويب المطلوب

## ملاحظات مهمة

- زر "بسط" يظهر فقط في تبويب المشكلة
- زر "التالي" مخفي في تبويب المشكلة
- المودال يمنع اختيار الحالة الحالية
- يتم تحديث العدادات تلقائياً بعد النقل
- رسائل نجاح/خطأ تظهر للمستخدم

## استكشاف الأخطاء

### إذا لم يظهر زر بسط:
1. تأكد من أنك في تبويب "مشكلة"
2. تحقق من أن الطلب في حالة مشكلة
3. أعد تحميل الصفحة

### إذا فشل API:
1. تحقق من أن الطلب في حالة مشكلة
2. تأكد من صحة الحالة المطلوبة
3. راجع سجلات التطبيق

### إذا لم يفتح المودال:
1. تحقق من console للأخطاء
2. تأكد من تحميل المكونات
3. أعد تشغيل التطبيق

## الخلاصة

زر "بسط" يوفر طريقة سهلة ومرنة لحل مشاكل الطلبات ونقلها للحالة المطلوبة، مما يحسن من كفاءة إدارة الطلبات في النظام.
